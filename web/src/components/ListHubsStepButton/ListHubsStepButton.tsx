import { Button } from '@buildcities/build-ui.components.all'
const ListHubsStepButton = ({}) => {
  return (
    <div className="mt-6">
      <Button onClick= type="submit" text="Next" />
    </div>
  )
}

export default ListHubsStepButton
