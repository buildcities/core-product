import BookReservationPage from './BookReservationPage'

const id = 1

export const generated = () => {
  return <BookReservationPage id={id} />
}

export default { title: 'Pages/BookReservationPage' }
