# Force redeploy
name: "development pipeline"
on:
  push:
     branches-ignore:
        - "main"
jobs:
  ## test
  call_test_workflow:
    uses: buildcities/composite-workflows/.github/workflows/test.yml@main
    secrets:
      token: ${{ github.token }}

